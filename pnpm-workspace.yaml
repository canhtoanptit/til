packages:
  - "apps/*"
  - "packages/*"

# Native/postinstall builds pnpm 10 must run (better-sqlite3 compiles the test
# harness's in-memory SQLite; esbuild/workerd install their platform binaries).
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  - workerd
